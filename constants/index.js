const {
  emailFrom,
  file,
  errorMessage,
  expiresIn,
  fileFormats,
  serverMessage,
  emailRegex,
  phoneRegex,
  passwordRegex,
  schemaMessage,
  cityRegex,
  defaultAvatarUrl,
} = require("./constants");

module.exports = {
  emailFrom,
  file,
  errorMessage,
  expiresIn,
  fileFormats,
  serverMessage,
  emailRegex,
  passwordRegex,
  phoneRegex,
  schemaMessage,
  cityRegex,
  defaultAvatarUrl,
};
