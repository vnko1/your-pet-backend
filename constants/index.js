const {
  emailFrom,
  file,
  errorMessage,
  expiresIn,
  fileFormats,
  serverMessage,
  emailRegex,
  phoneRegex,
  passwordRegex,
  schemaMessage,
} = require("./users/constants");

module.exports = {
  emailFrom,
  file,
  errorMessage,
  expiresIn,
  fileFormats,
  serverMessage,
  emailRegex,
  passwordRegex,
  phoneRegex,
  schemaMessage,
};
