const {
  emailFrom,
  file,
  errorMessage,
  expiresIn,
  fileFormats,
  serverMessage,
  emailRegex,
  passwordRegex,
  schemaMessage,
} = require("./constants");

module.exports = {
  emailFrom,
  file,
  errorMessage,
  expiresIn,
  fileFormats,
  serverMessage,
  emailRegex,
  passwordRegex,
  schemaMessage,
};
