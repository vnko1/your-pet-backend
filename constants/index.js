const {
  emailFrom,
  file,
  errorMessage,
  fileFormats,
  serverMessage,
  emailRegex,
  phoneRegex,
  passwordRegex,
  schemaMessage,
  cityRegex,
  defaultAvatarUrl,
  userFieldType,
} = require("./constants");

module.exports = {
  emailFrom,
  file,
  errorMessage,
  fileFormats,
  serverMessage,
  emailRegex,
  passwordRegex,
  phoneRegex,
  schemaMessage,
  cityRegex,
  defaultAvatarUrl,
  userFieldType,
};
