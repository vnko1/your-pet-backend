const { fieldValidation, checkFile } = require("./fieldValidation");
const { authentificate } = require("./authentification");
const { checkUserData } = require("./checkData");

module.exports = {
  fieldValidation,
  checkFile,
  authentificate,
  checkUserData,
};
